extends layout

block content
  main
    header
      h1 상담 플랫폼 – 시작하기
      p 역할을 선택하고 이름을 입력하신 뒤, 새 방을 만들거나 전달받은 코드로 참여해 주세요.

    form(method="POST")
      fieldset
        legend 역할 선택
        label
          input(type="radio" name="role" value="client" checked)
          |  내담자
        label
          input(type="radio" name="role" value="counselor")
          |  상담사

      label(for="name") 이름
      input#name(type="text" name="name" placeholder="예: 홍길동" required)

      label(for="room") 방 코드(참여 시에만 입력)
      input#room(type="text" name="room" placeholder="예: 전달받은 20자리 코드")

      div
        button(type="submit" formaction="/create") 새 방 만들기
        button(type="submit" formaction="/enter") 코드로 참여
